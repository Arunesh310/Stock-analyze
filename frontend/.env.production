# Live production frontend (Vercel) talks to the Render-hosted backend.
NEXT_PUBLIC_API_URL=https://bharatquant-backend.onrender.com
# Disable WebSocket from the Vercel frontend — Render free tier doesn't keep
# WS connections stable across cold starts. The dashboard falls back to
# polling automatically when this is empty.
NEXT_PUBLIC_WS_URL=
